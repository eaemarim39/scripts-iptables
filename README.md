# -scripts-iptables
Este script configura regras básicas de segurança usando iptables, incluindo a configuração de políticas de firewall padrão, regras para SSH, HTTP, HTTPS, DNS, além de limitar tentativas de conexão SSH e registrar tentativas de acesso negadas, ele também salva as regras configuradas no sistema.
